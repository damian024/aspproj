@model WebApplication1.Models.Event

@{
    Layout = "~/Views/Shared/_LayoutEventy.cshtml";
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Event</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.About)</dt>
        <dd> @Html.DisplayFor(model => model.About)</dd>
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd> @Html.DisplayFor(model => model.Name)</dd>
        <dt> @Html.DisplayNameFor(model => model.StartDate)</dt>
        <dd> @Html.DisplayFor(model => model.StartDate)</dd>
        <dt> Sponsorzy</dt>
        <dd></dd>
<dd>
    <a asp-controller="EventSponsors" asp-action="Create" asp-route-id="@Model.ID"> Add new sponsor </a>
            <table class="table">
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
                @foreach (var item in Model.EventSponsors)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Sponsor.Name)
                        </td>
                        <td>
                            <a asp-controller="EventSponsors" asp-action="Delete"asp-route-id="@item.EventSponsorID">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
